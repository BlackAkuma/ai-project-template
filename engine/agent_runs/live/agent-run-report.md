# Governed Agent Run — SPIKE-1

- ✅ **plan** [ok] **Step 1:** Create `calc.py` with the following code:
```python
def add(a, b):
    return a + b
```

**Step 2:** Verify it works by running in your terminal:
```bash
python -c "from calc import add; print(add(3, 5))"  # Output: 8
```
- ✅ **write** [ok] calc.py 69B (gated before write)
- ✅ **test** [green] exit=0
- ✅ **done** [ok] []

result: DONE (closed via Task Close Gate)